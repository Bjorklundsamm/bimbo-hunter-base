# Paimon Discord Bot Requirements
# Core Discord bot functionality
discord.py>=2.5.0

# Environment variables
python-dotenv==1.0.0

# HTTP requests for API calls
requests==2.31.0

# LangChain and LangGraph for agentic workflow (updated for Python 3.13 compatibility)
langchain>=0.2.0
langchain-anthropic>=0.1.15
langgraph>=0.2.0

# Anthropic Claude API
anthropic>=0.25.0

# Database (SQLite is built-in to Python)
# No additional requirements needed for SQLite
